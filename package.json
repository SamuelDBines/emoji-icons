{
  "name": "@samuelbines/emojis",
  "description": "",
  "main": "dist/index.js",
  "version": "0.0.1",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest run",
    "prebuild": "bun scripts/build.ts",
    "build": "tsc -p tsconfig.json",
    "demo": "bun run build:demo && bunx serve . -l 5173",
    "build:demo": "bun run build && rm -rf demo/dist && cp -R dist demo/dist",
    "dev": "bun scripts/build.ts && tsc -w -p tsconfig.json"
  },
  "keywords": [],
  "author": "Samuel Bines",
  "license": "ISC",
  "packageManager": "pnpm@10.20.0",
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  }
}